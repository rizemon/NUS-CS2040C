#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, m, TC;
  cin >> TC;
  int a ,b;
  while(TC--){
    cin >> n >> m;
    while(m--){
      cin >> a >> b;
    }
    cout << n-1 << endl;
  }
}