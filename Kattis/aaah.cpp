#include <bits/stdc++.h>
using namespace std;
int main(){
    string first,second;
    cin >> first;
    cin >> second;
    if(first.size()>=second.size()){
        cout<<"go"<<endl;
    }else{
        cout<<"no"<<endl;
    }
    return 0;
}